# Auto detect text files and perform LF normalization
* text=auto

# PowerShell files must always check out with CRLF (signed on Windows)
*.ps1  text eol=crlf
*.psm1 text eol=crlf
*.psd1 text eol=crlf
